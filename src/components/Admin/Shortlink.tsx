import type { NextPage } from "next";
import { IDashboardProps } from "../../interfaces/props/Dashboard";

export const Shortlink: NextPage<IDashboardProps> = (props) => {
	return <h1>Dashboard Shortlink</h1>;
};
